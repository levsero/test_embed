import { FONT_SIZE } from 'classicSrc/constants/shared'
import styled from 'styled-components'

const TicketFormTitle = styled.div`
  margin-bottom: ${10 / FONT_SIZE}rem !important;
`

export { TicketFormTitle }
