import { createAction } from '@reduxjs/toolkit'

export const messengerConfigReceived = createAction('messengerConfigReceived')
export const zIndexUpdated = createAction('zIndexUpdated')
export const userLoggedIn = createAction('userLoggedIn')
export const userLoggedOut = createAction('userLoggedOut')
