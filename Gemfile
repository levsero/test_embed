source 'https://rubygems.org'

group :deployment do
  gem 'aws-sdk'
  gem "airbrake"
end

source 'https://gem.zdsys.com/gems/' do
  group :deployment do
    gem 'zendesk_deployment', '~> 3.4'
  end
end
