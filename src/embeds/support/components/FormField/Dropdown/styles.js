import styled from 'styled-components'
import { Select } from '@zendeskgarden/react-dropdowns'

const SupportSelect = styled(Select)`
  &&& {
    margin-bottom: 0 !important;
  }
`

export { SupportSelect }
